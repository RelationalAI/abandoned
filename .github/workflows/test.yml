name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: RelationalAI/abandoned@main
        with:
          days: 7
          token: ${{ github.token }}
